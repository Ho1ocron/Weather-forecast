# Django project

1. Starting the project

    ```bash
    django-admin startproject project
    ```

2. Creating an app within the project

    ```bash
    python project/manage.py startapp myapp
    ```
